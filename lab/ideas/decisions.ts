// The selector's decisions: one per idea, saying whether it is built, held in
// reserve, or cut. The lead's replacement swaps edit this file too. One
// writer at a time; empty until the selection step fills it.

import type { Decision } from './types.ts'

export const decisions: readonly Decision[] = []
