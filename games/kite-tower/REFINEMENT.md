# Kite Tower: thirty refinement passes

> Progress: pass 6 done (towers that never sleep). Next: pass 7 (touch cue).

**How each pass was measured.** The refinement still is a fixed busy playroom seeded into storage (perch 0 on the shelf board, two cubes side by side with a third on top, an orange arch alone on the far side of the rug). It is taken at 1180×820, DPR 2, 6.4 seconds of game time after load. The page clock is frozen and stepped 33 ms at a time, so game time matches wall time on a 60 fps tablet however slowly this VM's software GL renders; the glow and the ghost hand are in frame. Perf is the shared jam probe (`perf-probe.mjs`) against the production build in Chromium with CDP CPU throttling at 6× (and 4× for the final table), 1180×820, DPR 2, touch, twelve taps across the screen. The VM has no GPU, so frame rate is software-GL fill rate. The CPU numbers (`cpuP50Ms`, `cpuP95Ms`: update plus render submit) and the renderer's draw calls are the proxies that carry over to a real iPad. Motion was reviewed on clock-driven recordings stitched at 30 fps, not on stills.

**A reset, and a redo.** The first run did passes 0–25 on this VM, and the VM was reset before they were published. Only the playable build from before the passes survived on the remote branch. Every pass below was then re-implemented on that branch and re-measured. The critiques come from the first run's screenshots, recordings and profiles, and each one was re-checked on the redone build before its fix went in.

| Pass | Critique (what reads badly, looks cheap, is unclear at 5, or is slow) | Change | Perf (6× cpu p50 / p95 · draws) |
| --- | --- | --- | --- |
| 0: before | Baseline on the new base. SwiftShader is GPU-bound at 1.2 fps at the default tier, and the governor never leaves tier 1: each 700–950 ms frame is thrown away as a "stall" by its 250 ms filter, the exact bug the adaptive-quality doc records. | Baseline | 7.3 / 15.6 ms · 41 draws (1.2 fps) |
| 1: governor | The governor contradicts the perf doc: a smoothed interval, a 250 ms stall filter and its own thresholds. On a slow device it never sees a sample, so it never steps down. | Pebble Table's thresholds: windows of 40 frames, a frame over 20 ms is dropped, a window with more than 10 % dropped is bad, two bad windows (or one averaging over 26 ms) step down, six clean windows under the 8 ms CPU budget step up, twice as many after a step up that failed, a 1000 ms stall filter, one settle window after each change, and touch starts one tier down. One extension, tested: a window also closes after 2 s of frames once it has four, so a device under 20 fps is judged in seconds, not minutes. Seven governor tests replace the old five. | 6.3–6.9 / 16.3–18.7 ms · 39 draws (1.4 fps; reaches minimal inside the probe) |
| 2: wood fill cost | Bisecting with objects hidden (first run): the merged wood mesh alone is most of the frame. The wood material reads the 1024² atlas five times per pixel with 4× anisotropy: albedo, roughness (the same texel again), and three taps for three's bump map. | One atlas read per pixel: roughness from that sample's green channel, grain relief from the screen-space slope of its red channel (Mikkelsen's unparametrized bump, the same one-pixel difference three takes with three extra reads). Crops of the tray and stack side by side read the same at play size. | 6.7–7.0 / 12.7–13.5 ms · 39 draws (2.6 fps) |
| 3: frame-budget test | Nothing in CI would catch a collider or planner regression; the perf doc asks for a budget test. | `perf.test.ts`: all twelve pieces in one leaning column collapse through the real controller for 6 s; the best-of-five average must stay under 0.75 ms a frame (measured 0.32 ms). | No runtime change |
| 4: MSAA | The canvas asks for 4× MSAA on top of DPR 1.5–2; the perf doc and Pebble Table run without it, and a software rasteriser pays for every sample. | `antialias: false`. At DPR 2 the bevelled edges already shade off softly; checked again at DPR 1 in the next pass's still, where only the thin tail strings step a little. | 5.0–5.1 / 10.1–11.6 ms · 39 draws (3.5–3.8 fps) |
| 5: colliders | Every load logs a dozen cannon-es "face normal points into the shape" errors: arch and half-moon segments were extruded in the piece's centre-of-mass frame, which lies outside most segments, so cannon's inward-normal check fails and each hull's bounding sphere is inflated. | Each convex part is extruded around its own centre and attached to the body at an offset. The console is clean; stacking, topple and frame-budget tests unchanged. | 5.0–5.5 / 10.5–12.3 ms · 39 draws (3.5–3.6 fps); within run-to-run noise |
| 6: towers that never sleep | A CPU profile at 6× (first run) had physics as the biggest script cost with nothing touched. A new test shows why: a straight four-high tower with the doll's weight on top never falls asleep at cannon's 0.1 sleep speed. It creeps a hair a frame, walks 0.15 units in 6 s, and keeps the solver running forever, and the doll on top rides the creep. | Sleep speed limit 0.4: the loaded tower jiggles, falls asleep and then stays put (no creep over the next 4 s); at 0.1 it is still awake after 10 s. Two new tests (the loaded tower sleeps and stops creeping, which fails at 0.1; a plank bridges two cubes under the doll); the topple tests are unchanged. | 5.4–5.9 / 10.7–11.9 ms · 39 draws (3.4–4.1 fps); the probe's taps keep everything awake, so the saving shows at rest, not here |
