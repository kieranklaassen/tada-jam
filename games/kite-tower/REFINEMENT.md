# Kite Tower: thirty refinement passes

> Progress: pass 20 done (cold playtest, and a reach that reads). Next: iter-20 still, publish, then pass 21.

**How each pass was measured.** The refinement still is a fixed busy playroom seeded into storage (perch 0 on the shelf board, two cubes side by side with a third on top, an orange arch alone on the far side of the rug). It is taken at 1180×820, DPR 2, 6.4 seconds of game time after load. The page clock is frozen and stepped 33 ms at a time, so game time matches wall time on a 60 fps tablet however slowly this VM's software GL renders; the glow and the ghost hand are in frame. Perf is the shared jam probe (`perf-probe.mjs`) against the production build in Chromium with CDP CPU throttling at 6× (and 4× for the final table), 1180×820, DPR 2, touch, twelve taps across the screen. The VM has no GPU, so frame rate is software-GL fill rate. The CPU numbers (`cpuP50Ms`, `cpuP95Ms`: update plus render submit) and the renderer's draw calls are the proxies that carry over to a real iPad. Motion was reviewed on clock-driven recordings stitched at 30 fps, not on stills.

**A reset, and a redo.** The first run did passes 0–25 on this VM, and the VM was reset before they were published. Only the playable build from before the passes survived on the remote branch. Every pass below was then re-implemented on that branch and re-measured. The critiques come from the first run's screenshots, recordings and profiles, and each one was re-checked on the redone build before its fix went in.

| Pass | Critique (what reads badly, looks cheap, is unclear at 5, or is slow) | Change | Perf (6× cpu p50 / p95 · draws) |
| --- | --- | --- | --- |
| 0: before | Baseline on the new base. SwiftShader is GPU-bound at 1.2 fps at the default tier, and the governor never leaves tier 1: each 700–950 ms frame is thrown away as a "stall" by its 250 ms filter, the exact bug the adaptive-quality doc records. | Baseline | 7.3 / 15.6 ms · 41 draws (1.2 fps) |
| 1: governor | The governor contradicts the perf doc: a smoothed interval, a 250 ms stall filter and its own thresholds. On a slow device it never sees a sample, so it never steps down. | Pebble Table's thresholds: windows of 40 frames, a frame over 20 ms is dropped, a window with more than 10 % dropped is bad, two bad windows (or one averaging over 26 ms) step down, six clean windows under the 8 ms CPU budget step up, twice as many after a step up that failed, a 1000 ms stall filter, one settle window after each change, and touch starts one tier down. One extension, tested: a window also closes after 2 s of frames once it has four, so a device under 20 fps is judged in seconds, not minutes. Seven governor tests replace the old five. | 6.3–6.9 / 16.3–18.7 ms · 39 draws (1.4 fps; reaches minimal inside the probe) |
| 2: wood fill cost | Bisecting with objects hidden (first run): the merged wood mesh alone is most of the frame. The wood material reads the 1024² atlas five times per pixel with 4× anisotropy: albedo, roughness (the same texel again), and three taps for three's bump map. | One atlas read per pixel: roughness from that sample's green channel, grain relief from the screen-space slope of its red channel (Mikkelsen's unparametrized bump, the same one-pixel difference three takes with three extra reads). Crops of the tray and stack side by side read the same at play size. | 6.7–7.0 / 12.7–13.5 ms · 39 draws (2.6 fps) |
| 3: frame-budget test | Nothing in CI would catch a collider or planner regression; the perf doc asks for a budget test. | `perf.test.ts`: all twelve pieces in one leaning column collapse through the real controller for 6 s; the best-of-five average must stay under 0.75 ms a frame (measured 0.32 ms). | No runtime change |
| 4: MSAA | The canvas asks for 4× MSAA on top of DPR 1.5–2; the perf doc and Pebble Table run without it, and a software rasteriser pays for every sample. | `antialias: false`. At DPR 2 the bevelled edges already shade off softly; checked again at DPR 1 in the next pass's still, where only the thin tail strings step a little. | 5.0–5.1 / 10.1–11.6 ms · 39 draws (3.5–3.8 fps) |
| 5: colliders | Every load logs a dozen cannon-es "face normal points into the shape" errors: arch and half-moon segments were extruded in the piece's centre-of-mass frame, which lies outside most segments, so cannon's inward-normal check fails and each hull's bounding sphere is inflated. | Each convex part is extruded around its own centre and attached to the body at an offset. The console is clean; stacking, topple and frame-budget tests unchanged. | 5.0–5.5 / 10.5–12.3 ms · 39 draws (3.5–3.6 fps); within run-to-run noise |
| 6: towers that never sleep | A CPU profile at 6× (first run) had physics as the biggest script cost with nothing touched. A new test shows why: a straight four-high tower with the doll's weight on top never falls asleep at cannon's 0.1 sleep speed. It creeps a hair a frame, walks 0.15 units in 6 s, and keeps the solver running forever, and the doll on top rides the creep. | Sleep speed limit 0.4: the loaded tower jiggles, falls asleep and then stays put (no creep over the next 4 s); at 0.1 it is still awake after 10 s. Two new tests (the loaded tower sleeps and stops creeping, which fails at 0.1; a plank bridges two cubes under the doll); the topple tests are unchanged. | 5.4–5.9 / 10.7–11.9 ms · 39 draws (3.4–4.1 fps); the probe's taps keep everything awake, so the saving shows at rest, not here |
| 7: touch cue | Idle a few seconds and whatever can be touched should say "here". The cue is an additive pale-yellow blob: on the cream rug and beech tray it clips to a white smudge and reads as a lighting glitch, not an invitation. | The cue is a honey ring (`#f0a73a`) with a faint core, drawn with normal blending around the tray piece and on the rug spot where one more piece would help. It still breathes and still clears on any touch. Same instanced layer, same draw. | 5.0–5.6 / 9.2–11.2 ms · 39 draws (3.6–3.8 fps) |
| 8: sunbeam and shadow sheets | The sunbeam costs two draws: three.js draws a transparent double-sided material back faces first, then front faces, so the largest translucent quad on screen is filled two times. The baked floor and rug shadows are two more draws of the same texture. | The beam is forced to a single pass (it is additive, so face order cannot change the result), and the floor and rug shadow sheets are merged into one mesh. The still is unchanged. | 4.9–5.2 / 9.1–9.9 ms · 37 draws (3.8–3.9 fps) |
| 9: collision cost | Physics is still the biggest script cost while pieces move. cannon-es tests every hull's face normals and then every pair of edge directions. For the flat prisms here, the edge pairs only give back the side normals or the z axis, and z can never separate two pieces that all live on z = 0. A half-moon touching an arch is six hull pairs of about 75 axis projections each, every step. | Each hull tests only its own in-plane side normals (parallel ones once) and has no edge-pair list, so that pair drops to about 16. A new test pins this (it fails on the old hulls). The controller topple benchmark goes from 0.115 to 0.074 ms a frame (a spread of settling pieces from 0.069 to 0.049). Also tried an 8-facet half-moon hull instead of 12: no measurable gain, so it was reverted. | 4.1–4.4 / 8.0–9.8 ms · 37 draws (3.6–4.1 fps) |
| 10: the shape behind Pip | Pip stands in front of the shelf, and the bottom board holds a nested rainbow in saturated red, orange and yellow: from the child's seat it reads as one of her blocks (the tray has a red arch too), and its red cuts straight into her head and dress. | A ring stacker in pale cool stains (sky, sea green, lilac) on a beech peg replaces it, merged into the same wood mesh. Pip's head now reads against the pale shelf back. Background toys stay lighter and cooler than anything the child can pick up. | 4.0–4.6 / 10.9–11.8 ms · 37 draws (3.7 fps) |
| 11: a personality in each doll's motion | Watch the dolls for a minute (first-run recording) and they are three copies of one puppet: each has one reaction, one cheer and one poke, every doll blinks on the same fixed beat, nobody does anything on their own while the child thinks, and Pip's only answer to a tap is the same spin. The motion-personality doc names exactly this. | A pure `motion.ts`: a personality per doll (Pip curious and quick, Moss slow and warm, Bean fizzy) with its own look speed, reaction delay, blink rhythm, walk and three or four actions for each of react, cheer, poke and idle delight. A `MotionDirector` per doll never repeats the last action, varies size ±15 % and speed ±10 %, plays a small delight every few seconds only while the room is calm, and blinks on a jittered beat with a 20 % double blink. Eleven tests: no two actions are near-copies over 21 samples, every action starts and ends at rest, Moss < Pip < Bean in tempo, look speed and blink speed, no repeats, delights stop while walking or busy, and react delay differs per doll. Reviewed on a clock-driven recording of each doll poked twice. | 4.5–5.5 / 9.1–9.8 ms · 37 draws (3.7–3.9 fps) |
| 12: the grown-up overlay | A parent or tester on an iPad has no way to see why it stutters: the only readout is a `?fps=1` bar graph with no tier, DPR, draw count or dropped frames, and no way to pin a tier to compare. The perf doc asks for exactly that behind a triple tap. | Triple-tap the bare top-left 72 px corner within 700 ms (or open with `?fps=1`): the CPU bar graph against the 8 ms budget, then fps, frame ms, CPU p95 and dropped/judged frames, then the tier (auto or pinned), DPR, draws and triangles, and buttons that pin full, balanced, lean or minimal or hand the tier back to the governor. The corner swallows its taps, so it never picks up a piece. A new governor test: any tier can be pinned and handed back. Checked on a scenario that opens it, pins minimal (38 draws), pins full, returns to auto and closes it, and on a real clock (cpu p95 3.1 ms, the governor stepping down on its own). | 4.3–4.6 / 9.6–10.9 ms · 37 draws (3.8–3.9 fps) |
| 13: the wait before she climbs | Drop a piece next to Pip and she waits for the tower to rest before she steps on. With her weight on a straight four-high tower the pieces keep jiggling just above the sleep speed for 3–6 s without going anywhere, so a five-year-old sees a doll who ignores the block they just gave her, and the solver runs the whole time. | A body that stays within 0.04 units and 0.04 rad of one pose for 0.8 s is put to sleep; a real topple leaves that window long before. Loaded-tower rest goes from 3.2–6.1 s to 1.0–1.7 s across four offsets of the doll, and the top piece creeps less while it waits (0.04–0.14 units, was 0.09–0.23). The loaded-tower test now also asks for rest inside 2 s; the topple and frame-budget tests are unchanged. | 4.5 / 9.3–11.9 ms · 37 draws (3.6–3.8 fps); the probe's taps keep pieces moving, so the saving shows at rest |
| 14: answers you can see from the seat | The pass 11 recording, watched as frame strips cut from where each doll stands at play size (dolls are about 70 px tall): Moss's belly-chuckle moves his arms in front of his own green coat and reads as nothing, his slow-clap is the same, and Bean's giggle-wiggle is a head tilt. The doll was poked and did not answer. The doc records the same miss (a bear's belly pat). | Every answer changes the outline. Moss's arms swing out from his belly on each heave of the laugh and open wide between claps; Bean flaps both arms out while he wiggles; Pip's shy-sway turns her shoulder further and her giggle-hop leaves the floor; Moss's wince and Bean's gasp lean further; five quiet idle delights move the body, not just the head. A new test reads each pose from the front (an arm out to the side, a hop, a lean, a turn; arms in front and head tilts count for nothing) at the director's smallest size: every react, cheer and poke must reach a full read and every delight half of one. Before this change it failed on twelve actions (seven answers, five delights). Re-recorded: all six pokes read in the strips. | 4.0–4.2 / 8.5–11.8 ms · 37 draws (3.8–4.1 fps) |
| 15: sound on the first touch and on every landing | A CPU profile of the probe's taps at 6×: the tok for a landing piece builds nine audio nodes with twelve scheduled envelope events inside the frame (`tone` and `burst` together 22 ms across twelve taps), and the child's first touch fills a second of white noise and a stereo room impulse (90k random numbers and powers) on top of creating the context. Worse, the sound is unlocked only on pointer down, and for a finger WebKit and Chrome count only the lift as a gesture: on an iPad the room could stay silent. | The noise is filled in 4096-sample slices after load and reused for the room impulse, so the first touch only copies it. Each piece's tok is rendered once with an `OfflineAudioContext` after load (six buffers, peaks 0.49–0.61, checked in the browser) and a landing plays one buffer source through one gain, with pitch jitter by playback rate; until the bake finishes, the same synthesis runs live. The touch unlocks the sound on the way up too, as Pebble Table does, with a test. `tone` and `burst` leave the profile's top thirty. Creating the context itself (about 45 ms at 6×, native) stays inside the touch, where iOS needs it. | 3.8–4.4 / 7.9–9.2 ms · 37 draws (4.0–4.1 fps) |
| 16: where did the kite go? | Stills every 0.4 s through a whole round (climb, grab, flight, landing, drift): the flight reads, but at the end of its 3.4 s drift the kite eases to zero speed and simply stops on its new perch. Nothing marks the moment the next challenge is set, and a five-year-old who looked at Pip on landing does not see where it went. | On arrival the kite shivers for a second as it catches (the same shiver as a tap) and gives its soft rustle. A new controller test: after a full round, the shiver starts on the frame the drift ends and the rustle comes after the landing thump. Checked on stills every 100 ms at the peg rail. | 4.4–4.6 / 8.5–9.7 ms · 37 draws (3.9–4.1 fps) |
| 17: she saw what I did | Drag a block to Pip and every head follows it while it is held, then snaps back the instant the finger lifts: Pip to the kite, Moss and Bean to Pip. Nobody watches the block land, so the child's gift goes unseen at the one moment it matters. | For 1.4 s after a piece is let go of (dropped or tapped out of the tray), Pip and both watchers keep looking at it while it falls and settles; Pip turns to face it, then goes back to the kite. A new controller test: 0.4 s after the drop all three are looking at the piece, and 1.5 s later none are. Checked on stills of a teal cube dropped left of the lamp: Pip turns round to it, Bean's head follows it, then Pip reaches for the kite again. | 4.0 / 7.5–8.2 ms · 37 draws (4.1 fps) |
| 18: the room is there sooner | A load probe (long tasks from navigation to the first frame, 6×, cold storage): 2.8 s of main-thread tasks before the first frame, one of them 1.9 s, so a tap in the first two seconds does nothing. The profile puts 1.3 s of it in the wood atlas's value noise: four corner hashes for each of nine noise lookups per pixel, a million pixels, plus a sine per pixel. | Along one row of pixels y is fixed, so each noise cell's y-blend is done once per row and a pixel only eases between two of them; the long grain's slow sway splits into a column sine and a row sine. The atlas is byte-for-byte the same (a throwaway check built both versions: 0 of 4,194,304 bytes differ) and builds 2.2× faster (208 to 93 ms in Node). New tests hold the row lookups to the corner version at every pixel and across the wrap. Interleaved A/B at 6×: longest task 1.9 to 1.2 s, main-thread tasks before the first frame 2.8 to 2.1 s, first frame 5.8–6.1 to 5.0–5.5 s. Also tried batching the rug's 90,000 fibre strokes into one path per colour: it saved under 0.1 s of script but made the first frame about 1 s later (one huge path rasterises slowly), so it was reverted. | 3.8–5.7 / 7.2–11.9 ms · 37 draws (4.3–4.6 fps; four runs, frame loop unchanged) |
| 19: the lowest tier is still the room | The refinement still pinned to minimal (`?tier=3`), where the governor ends up on this VM and where a struggling tablet would land, beside the same still at full. At play size it is the same game but paler at the edges: the centre of the frame matches (mean 224/202/169 against 223/201/166) while the top-left corner goes from 194/183/160 to 209/203/186, bright and cool, because lean and minimal drop the warm grade and vignette. The checklist asks that the lowest tier still look like the game. | The grade stays on every tier. It is one triangle multiplied over the frame with no render target, so at DPR 1 it is cheap: pinned minimal at 6× it costs 5.75 to 5.6 fps in software GL (about 3%) and one draw, with CPU unchanged within noise. Tiers now shed DPR, motes, tail links and substeps only. Re-shot pinned minimal: the corner and the whole-frame means now equal full's (194/183/160 and 213/189/155). | 3.8–3.9 / 7.3–7.9 ms · 38 draws (4.1–4.4 fps) |
| 20: cold playtest, and a reach that reads | The cold playtest proxy on the production build, fresh storage, clock-driven stills. Hands off for 10 s, the scene invites: Pip at the shelf with both arms up toward the stuck kite from the first frame, the yellow cube hopping in the tray at 2 s, a glow at her feet at 3 s, the ghost hand carrying a see-through cube to her at 5 s. Then as a newcomer: doing what the hand showed makes her climb, grab the kite and fly round the room, and the kite shivers onto the picture hook with Pip under it reaching again; the next hand shows a green cube beside her, then a blue one on the rug beside that (steps, not a pillar). Stacking straight onto the cube she stands on instead makes her somersault off with a whee and a giggle, which reads as play. Unclear moments: one. On the contact sheets (the doll about 70 px tall), her reach read as holding her head: short peg arms raised straight up end at the top of a big head. | The arm on the kite's side stretches up and out toward it, clear of the head; the other stays out for balance (both up read as a cheer); she leans in a little and keeps facing the child. Tried first: leaning and turning the whole doll toward the kite, which hid her face and pushed her head behind a tower beside her, so it went. Re-shot the three reaches from the playtest (at the shelf, on a cube under the hook, on the rug beside a two-cube tower): the first two now read as reaching for the kite; beside the tower her kite-side arm is hidden by the blocks. Also stress-probed the physics with all twelve pieces collapsing from a leaning column (see "Still weak"). | 4.3–4.9 / 8.1–9.6 ms · 38 draws (4.3–4.4 fps) |

## Still weak

- **A full collapse on a slow device.** All twelve pieces tumbling from a leaning column, Chromium at 6× with SwiftShader: frame CPU runs p50 8–10 ms and peaks at 19–20 ms for the second or two it lasts, because every slow software-GL frame runs the tier's two or three catch-up physics substeps. In Node one collapse step is p95 0.21 ms and worst 1.0–1.4 ms, so a 60 Hz tablet running one substep a frame should stay near its normal budget. That is an inference; no physical iPad was measured.
- **A reach behind a tower.** When Pip stands beside a tower that hides the kite's side of her, her stretching arm is behind the blocks and only the balancing arm shows.
