import type { CartridgeManifest } from '../types'

export const cosyScarfManifest: CartridgeManifest = {
  key: 'cosy-scarf',
  name: 'Cosy Scarf',
  ageBand: [5, 10],
  permissions: ['storage'],
  iconIdentity: { family: 'make', contrast: 'paper' },
  author: 'Kieran Klaassen',
  version: '0.1.0',
}
